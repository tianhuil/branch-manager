{
  "name": "neon-prototype",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src",
    "web"
  ],
  "scripts": {
    "dev": "bun --cwd web run dev",
    "build": "bun --cwd web run build",
    "start": "bun --cwd web run start",
    "test": "bun test",
    "test:e2e": "dotenvx run -f .env.root.local -f .env -- bun test ./src/**/*.e2e.ts",
    "lint": "bun --cwd src run lint && bun --cwd web run lint",
    "lint:fix": "bun --cwd src run lint:fix && bun --cwd web run lint:fix",
    "format": "prettier .",
    "format:fix": "prettier --write .",
    "typecheck": "bun --cwd src run typecheck && bun --cwd web run typecheck",
    "spell": "cspell \"**/*\"",
    "spell:fix": "cspell \"**/*\" --no-progress --no-summary",
    "check": "bun run lint && bun run format && bun run typecheck && bun run spell",
    "check:fix": "bun run lint:fix && bun run format:fix && bun run spell:fix",
    "bm": "bun --cwd src run index.ts"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.51.0",
    "cspell": "^9.2.1",
    "prettier": "^3.6.2",
    "vercel": "^48.2.8"
  }
}
